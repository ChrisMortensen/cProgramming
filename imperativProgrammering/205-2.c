#include <stdio.h>
#include <ctype.h>


void main(){
	/*
	//	a
	int between,n,k;
	scanf("%d %d",&n,&k);
	between = n<=k && n>=-k ? 0 : 1;
	*/
	/*
	//	b
	int divisor, digit, num;
	scanf("%d %d",&digit,&num);
	divisor = digit%num==0 ? 1 : 0;
	*/
	/*
	//	c
	char ch;
	int lowercase;
	scanf("%c",&ch);
	lowercase = islower(ch) ? 1 : 0;
	printf("%d",lowercase);
	*/

	return;
}